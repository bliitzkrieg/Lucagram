.posts-wrapper.row
  .post
    .post-head
      .name
        A User
    .image.center-block
      =image_tag @post.image.url(:medium)
    %p.caption
      =@post.caption
    .text-center
      =link_to "Edit", post_path(@post), method: :delete
      |
      =link_to "Cancel", posts_path